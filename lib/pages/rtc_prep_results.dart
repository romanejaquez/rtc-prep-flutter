import 'package:flutter/material.dart';

class RTCPrepExamResults extends StatelessWidget {

  static const String route = '/results';

  const RTCPrepExamResults({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
      child: Text('results'),
    )
    );
  }
}